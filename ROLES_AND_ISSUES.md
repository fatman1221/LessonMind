# 系统角色与问题分析

## 📋 系统角色列表

根据代码分析，系统中定义了以下角色：

### 1. **teacher（教师）** - 默认角色
- **角色标识**: `teacher`
- **默认角色**: 是（新注册用户默认为此角色）
- **权限范围**:
  - ✅ 使用AI对话功能
  - ✅ 生成和管理教学设计
  - ✅ 生成多媒体资源（图片、PPT）
  - ✅ 进行学情分析
  - ✅ 管理题库
  - ✅ 管理个人收藏
  - ✅ 查看自己的数据

### 2. **admin（管理员）**
- **角色标识**: `admin`
- **默认角色**: 否（需要通过 `init_admin.py` 创建）
- **权限范围**:
  - ✅ 拥有教师的所有权限
  - ❌ **缺少**: 用户管理功能（查看所有用户、编辑用户、删除用户）
  - ❌ **缺少**: 系统管理功能
  - ❌ **缺少**: 数据统计功能

---

## ⚠️ 发现的问题

### 🔴 严重问题

#### 1. **缺少基于角色的权限控制（RBAC）**
- **问题**: 后端API没有基于角色的权限验证
- **影响**: 所有用户（包括普通教师）都可以访问所有功能
- **位置**: 
  - `backend/api/*.py` - 所有API端点都只检查登录，不检查角色
- **建议**: 添加权限装饰器或依赖项来验证角色

#### 2. **前端没有保存用户角色信息**
- **问题**: 登录时只保存了 `username`，没有保存 `role` 信息
- **影响**: 前端无法根据角色显示不同的菜单或功能
- **位置**: 
  - `frontend/src/views/Login.vue` (第132行)
  - `frontend/src/stores/auth.js` - 缺少 `isAdmin` 计算属性
- **建议**: 登录后保存完整的用户信息，包括角色

#### 3. **缺少管理员专用功能**
- **问题**: 没有管理员专用的用户管理界面和API
- **影响**: 管理员无法管理系统用户
- **建议**: 创建管理员专用的用户管理功能

### 🟡 中等问题

#### 4. **前端菜单没有根据角色显示**
- **问题**: 所有用户看到相同的菜单
- **影响**: 管理员和普通教师看到相同的界面
- **位置**: `frontend/src/layouts/MainLayout.vue`
- **建议**: 根据用户角色动态显示菜单项

#### 5. **缺少角色验证的辅助函数**
- **问题**: 后端没有 `require_admin` 或 `require_role` 等辅助函数
- **影响**: 每次需要权限验证时都要重复写代码
- **建议**: 在 `backend/api/auth.py` 中添加权限验证辅助函数

#### 6. **用户注册时没有角色选择**
- **问题**: 所有新注册用户都是 `teacher` 角色
- **影响**: 无法通过注册创建管理员（这是合理的，但应该有说明）
- **位置**: `backend/api/auth.py` (第96行)

### 🟢 轻微问题

#### 7. **缺少角色枚举或常量定义**
- **问题**: 角色字符串硬编码在多个地方
- **影响**: 如果修改角色名称，需要修改多处代码
- **建议**: 在 `backend/models.py` 或 `backend/config.py` 中定义角色常量

#### 8. **缺少用户状态管理（is_active）的使用**
- **问题**: 虽然模型中有 `is_active` 字段，但没有在登录时检查
- **影响**: 被禁用的用户仍然可以登录
- **位置**: `backend/api/auth.py` (第115行)
- **建议**: 在登录验证时检查 `is_active` 状态

---

## 🔧 建议的改进方案

### 优先级 1（必须修复）

1. **添加权限验证函数**
   ```python
   # backend/api/auth.py
   async def require_admin(current_user: User = Depends(get_current_user)):
       if current_user.role != "admin":
           raise HTTPException(status_code=403, detail="需要管理员权限")
       return current_user
   ```

2. **修复前端用户信息保存**
   ```javascript
   // frontend/src/views/Login.vue
   authStore.setAuth(res.access_token, {
     username: loginForm.username,
     role: res.user?.role || 'teacher'  // 需要后端返回用户信息
   })
   ```

3. **添加管理员用户管理API**
   - 获取所有用户列表
   - 编辑用户信息
   - 删除用户
   - 修改用户角色

### 优先级 2（建议添加）

4. **前端角色权限控制**
   - 添加 `isAdmin` 计算属性
   - 根据角色显示/隐藏菜单项
   - 添加管理员专用页面

5. **登录时检查用户状态**
   ```python
   if not user.is_active:
       raise HTTPException(status_code=403, detail="账户已被禁用")
   ```

6. **定义角色常量**
   ```python
   # backend/models.py
   class UserRole:
       TEACHER = "teacher"
       ADMIN = "admin"
   ```

---

## 📊 当前系统状态总结

| 功能 | 教师(teacher) | 管理员(admin) | 状态 |
|------|--------------|--------------|------|
| 登录/注册 | ✅ | ✅ | 正常 |
| AI对话 | ✅ | ✅ | 正常 |
| 教学设计 | ✅ | ✅ | 正常 |
| 多媒体资源 | ✅ | ✅ | 正常 |
| 学情分析 | ✅ | ✅ | 正常 |
| 题库管理 | ✅ | ✅ | 正常 |
| 个人收藏 | ✅ | ✅ | 正常 |
| 用户管理 | ❌ | ❌ | **缺失** |
| 系统管理 | ❌ | ❌ | **缺失** |
| 数据统计 | ❌ | ❌ | **缺失** |

---

## 🎯 下一步行动建议

1. **立即修复**: 前端用户信息保存问题
2. **立即添加**: 后端权限验证函数
3. **短期计划**: 实现管理员用户管理功能
4. **长期计划**: 完善角色权限体系，支持更多角色（如：教研员、学校管理员等）

